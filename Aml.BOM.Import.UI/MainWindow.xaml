<Window x:Class="Aml.BOM.Import.UI.MainWindow"
xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
mc:Ignorable="d"
Title="BOM Import Utility" Height="700" Width="1200"
WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Navigation Panel -->
        <Border Grid.Column="0" Background="#2C3E50" Padding="10">
            <StackPanel>
                <TextBlock Text="BOM Import Utility" 
                          FontSize="16" 
                          FontWeight="Bold" 
                          Foreground="White" 
                          Margin="0,0,0,30"
                          TextWrapping="Wrap"/>

                <Button Command="{Binding NavigateCommand}"
                       CommandParameter="NewBoms"
                       Style="{StaticResource NavigationButtonStyle}"
                       Margin="0,0,0,5">
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconMaterial Kind="Package" Width="16" Height="16" Margin="0,0,8,0"/>
                        <TextBlock Text="New BOMs" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <Button Command="{Binding NavigateCommand}"
                       CommandParameter="NewBuyItems"
                       Style="{StaticResource NavigationButtonStyle}"
                       Margin="0,0,0,5">
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconMaterial Kind="Cart" Width="16" Height="16" Margin="0,0,8,0"/>
                        <TextBlock Text="New Buy Items" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <Button Command="{Binding NavigateCommand}"
                       CommandParameter="NewMakeItems"
                       Style="{StaticResource NavigationButtonStyle}"
                       Margin="0,0,0,5">
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconMaterial Kind="Hammer" Width="16" Height="16" Margin="0,0,8,0"/>
                        <TextBlock Text="New Make Items" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <Button Command="{Binding NavigateCommand}"
                       CommandParameter="IntegratedBoms"
                       Style="{StaticResource NavigationButtonStyle}"
                       Margin="0,0,0,5">
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconMaterial Kind="CheckCircle" Width="16" Height="16" Margin="0,0,8,0"/>
                        <TextBlock Text="Integrated BOMs" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <Button Command="{Binding NavigateCommand}"
                       CommandParameter="DuplicateBoms"
                       Style="{StaticResource NavigationButtonStyle}"
                       Margin="0,0,0,5">
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconMaterial Kind="ContentDuplicate" Width="16" Height="16" Margin="0,0,8,0"/>
                        <TextBlock Text="Duplicate BOMs" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <Separator Margin="0,20,0,20" Background="#34495E"/>

                <Button Command="{Binding NavigateCommand}"
                       CommandParameter="Settings"
                       Style="{StaticResource NavigationButtonStyle}">
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconMaterial Kind="Cog" Width="16" Height="16" Margin="0,0,8,0"/>
                        <TextBlock Text="Settings" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>

        <!-- Content Area -->
        <Border Grid.Column="1" Background="#ECF0F1" Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <StackPanel Grid.Row="0" Margin="0,0,0,20">
                    <TextBlock Text="{Binding CurrentViewTitle}" 
                              FontSize="24" 
                              FontWeight="Bold" 
                              Foreground="#2C3E50"/>
                    <TextBlock Text="{Binding CurrentViewDescription}" 
                              FontSize="12" 
                              Foreground="#7F8C8D"
                              Margin="0,5,0,0"/>
                </StackPanel>

                <!-- Dynamic Content -->
                <ContentControl Grid.Row="1" Content="{Binding CurrentViewModel}">
                    <ContentControl.Resources>
                        <!-- View templates will be registered here -->
                    </ContentControl.Resources>
                </ContentControl>
            </Grid>
        </Border>
    </Grid>
</Window>
