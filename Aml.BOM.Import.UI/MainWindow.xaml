<Window x:Class="Aml.BOM.Import.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="BOM Import Utility" Height="700" Width="1200"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Navigation Panel -->
        <Border Grid.Column="0" Background="#2C3E50" Padding="10">
            <StackPanel>
                <TextBlock Text="BOM Import Utility" 
                          FontSize="16" 
                          FontWeight="Bold" 
                          Foreground="White" 
                          Margin="0,0,0,30"
                          TextWrapping="Wrap"/>

                <Button Content="New Buy Items" 
                       Command="{Binding NavigateCommand}"
                       CommandParameter="NewBuyItems"
                       Style="{StaticResource NavigationButtonStyle}"
                       Margin="0,0,0,5"/>

                <Button Content="New Make Items" 
                       Command="{Binding NavigateCommand}"
                       CommandParameter="NewMakeItems"
                       Style="{StaticResource NavigationButtonStyle}"
                       Margin="0,0,0,5"/>

                <Button Content="New BOMs" 
                       Command="{Binding NavigateCommand}"
                       CommandParameter="NewBoms"
                       Style="{StaticResource NavigationButtonStyle}"
                       Margin="0,0,0,5"/>

                <Button Content="Integrated BOMs" 
                       Command="{Binding NavigateCommand}"
                       CommandParameter="IntegratedBoms"
                       Style="{StaticResource NavigationButtonStyle}"
                       Margin="0,0,0,5"/>

                <Button Content="Duplicate BOMs" 
                       Command="{Binding NavigateCommand}"
                       CommandParameter="DuplicateBoms"
                       Style="{StaticResource NavigationButtonStyle}"
                       Margin="0,0,0,5"/>

                <Separator Margin="0,20,0,20" Background="#34495E"/>

                <Button Content="Settings" 
                       Command="{Binding NavigateCommand}"
                       CommandParameter="Settings"
                       Style="{StaticResource NavigationButtonStyle}"/>
            </StackPanel>
        </Border>

        <!-- Content Area -->
        <Border Grid.Column="1" Background="#ECF0F1" Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <StackPanel Grid.Row="0" Margin="0,0,0,20">
                    <TextBlock Text="{Binding CurrentViewTitle}" 
                              FontSize="24" 
                              FontWeight="Bold" 
                              Foreground="#2C3E50"/>
                    <TextBlock Text="{Binding CurrentViewDescription}" 
                              FontSize="12" 
                              Foreground="#7F8C8D"
                              Margin="0,5,0,0"/>
                </StackPanel>

                <!-- Dynamic Content -->
                <ContentControl Grid.Row="1" Content="{Binding CurrentViewModel}">
                    <ContentControl.Resources>
                        <!-- View templates will be registered here -->
                    </ContentControl.Resources>
                </ContentControl>
            </Grid>
        </Border>
    </Grid>
</Window>
