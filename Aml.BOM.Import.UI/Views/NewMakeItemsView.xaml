<UserControl x:Class="Aml.BOM.Import.UI.Views.NewMakeItemsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Refresh" 
                   Command="{Binding RefreshCommand}"
                   Style="{StaticResource PrimaryButtonStyle}"
                   Margin="0,0,10,0"/>
            <Button Content="Bulk Edit" 
                   Command="{Binding BulkEditCommand}"
                   Style="{StaticResource SecondaryButtonStyle}"/>
        </StackPanel>

        <!-- Bulk Edit Panel -->
        <Border Grid.Row="1" 
               Background="White" 
               Padding="10" 
               Margin="0,0,0,10"
               BorderBrush="#BDC3C7"
               BorderThickness="1"
               CornerRadius="4">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Bulk Edit Selected Items:" 
                          VerticalAlignment="Center" 
                          Margin="0,0,10,0"/>
                <ComboBox Width="150" Margin="0,0,10,0">
                    <ComboBoxItem Content="Product Group"/>
                    <ComboBoxItem Content="Category"/>
                    <ComboBoxItem Content="Drawing Number"/>
                    <ComboBoxItem Content="Revision"/>
                </ComboBox>
                <TextBox Width="200" Margin="0,0,10,0" Text="{Binding BulkEditValue}"/>
                <Button Content="Apply" 
                       Command="{Binding BulkEditCommand}"
                       Style="{StaticResource PrimaryButtonStyle}"/>
            </StackPanel>
        </Border>

        <!-- Items Grid -->
        <DataGrid Grid.Row="2" 
                 ItemsSource="{Binding Items}"
                 SelectionMode="Extended">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Item Code" Binding="{Binding ItemCode}" Width="120"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                <DataGridTextColumn Header="UOM" Binding="{Binding UnitOfMeasure}" Width="60"/>
                <DataGridTextColumn Header="Product Group" Binding="{Binding ProductGroup}" Width="120"/>
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="100"/>
                <DataGridTextColumn Header="Drawing #" Binding="{Binding DrawingNumber}" Width="100"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Loading Overlay -->
        <Border Grid.Row="0" Grid.RowSpan="3" 
               Background="#80000000" 
               Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
            <TextBlock Text="Loading..." 
                      FontSize="20" 
                      Foreground="White" 
                      HorizontalAlignment="Center" 
                      VerticalAlignment="Center"/>
        </Border>
    </Grid>
</UserControl>
