<UserControl x:Class="Aml.BOM.Import.UI.Views.DuplicateBomsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Refresh" 
                   Command="{Binding RefreshCommand}"
                   Style="{StaticResource PrimaryButtonStyle}"
                   Margin="0,0,10,0"/>
            <Button Content="Delete Selected" 
                   Command="{Binding DeleteSelectedCommand}"
                   Style="{StaticResource SecondaryButtonStyle}"
                   Margin="0,0,10,0"/>
            <Button Content="Delete All Duplicates" 
                   Command="{Binding DeleteAllCommand}"
                   Background="#F44336"
                   Foreground="White"
                   Padding="15,8"
                   Margin="0,0,10,0"
                   BorderThickness="0"
                   Cursor="Hand"/>
        </StackPanel>

        <!-- Statistics Panel -->
        <Border Grid.Row="1" 
                Background="{StaticResource PrimaryBackgroundBrush}" 
                BorderBrush="{StaticResource BorderBrush}" 
                BorderThickness="1" 
                CornerRadius="5" 
                Padding="15" 
                Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Total Duplicate BOMs -->
                <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                    <TextBlock Text="Duplicate BOMs" 
                              FontSize="12" 
                              FontWeight="SemiBold" 
                              Foreground="{StaticResource SecondaryTextBrush}" 
                              HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding TotalDuplicateBoms}" 
                              FontSize="28" 
                              FontWeight="Bold" 
                              Foreground="#F44336" 
                              HorizontalAlignment="Center" 
                              Margin="0,5,0,0"/>
                </StackPanel>

                <!-- Unique Parent Items -->
                <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                    <TextBlock Text="Unique Parents" 
                              FontSize="12" 
                              FontWeight="SemiBold" 
                              Foreground="{StaticResource SecondaryTextBrush}" 
                              HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding UniqueParentItems}" 
                              FontSize="28" 
                              FontWeight="Bold" 
                              Foreground="#FF9800" 
                              HorizontalAlignment="Center" 
                              Margin="0,5,0,0"/>
                </StackPanel>

                <!-- Total Records -->
                <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                    <TextBlock Text="Total Records" 
                              FontSize="12" 
                              FontWeight="SemiBold" 
                              Foreground="{StaticResource SecondaryTextBrush}" 
                              HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding TotalDuplicateRecords}" 
                              FontSize="28" 
                              FontWeight="Bold" 
                              Foreground="{StaticResource PrimaryTextBrush}" 
                              HorizontalAlignment="Center" 
                              Margin="0,5,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Search Bar -->
        <Border Grid.Row="2" 
                Background="{StaticResource SecondaryBackgroundBrush}" 
                BorderBrush="{StaticResource BorderBrush}" 
                BorderThickness="1" 
                CornerRadius="5" 
                Padding="10" 
                Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0" 
                        Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                        VerticalContentAlignment="Center" 
                        Padding="10,5"
                        FontSize="14">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Style.Triggers>
                                <Trigger Property="Text" Value="">
                                    <Setter Property="Background">
                                        <Setter.Value>
                                            <VisualBrush Stretch="None" AlignmentX="Left">
                                                <VisualBrush.Visual>
                                                    <TextBlock Text="Search by Parent, Component, BOM#, File Name..." 
                                                             Foreground="Gray" 
                                                             Margin="10,0,0,0"/>
                                                </VisualBrush.Visual>
                                            </VisualBrush>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <Button Grid.Column="1" 
                       Content="Search" 
                       Command="{Binding SearchCommand}"
                       Style="{StaticResource PrimaryButtonStyle}"
                       Margin="5,0"/>

                <Button Grid.Column="2" 
                       Content="Clear" 
                       Command="{Binding ClearSearchCommand}"
                       Style="{StaticResource SecondaryButtonStyle}"/>
            </Grid>
        </Border>

        <!-- Duplicate BOMs Grid -->
        <DataGrid Grid.Row="3" 
                 ItemsSource="{Binding DuplicateBoms}"
                 SelectedItem="{Binding SelectedBom}"
                 AutoGenerateColumns="False"
                 IsReadOnly="True"
                 SelectionMode="Single"
                 CanUserSortColumns="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Parent Item Code" Binding="{Binding ParentItemCode}" Width="200"/>
                <DataGridTextColumn Header="Component Item Code" Binding="{Binding ComponentItemCode}" Width="200"/>
                <DataGridTextColumn Header="Import File Name" Binding="{Binding ImportFileName}" Width="300"/>
                <DataGridTextColumn Header="Import Date" Binding="{Binding ImportDate, StringFormat='yyyy-MM-dd HH:mm'}" Width="*"/>
            </DataGrid.Columns>
            
            <!-- Row Style for Duplicates -->
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="Background" Value="#FFEBEE"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#FFCDD2"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#FFCDD2"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
        </DataGrid>

        <!-- Status Bar -->
        <Border Grid.Row="4" 
                Background="{StaticResource SecondaryBackgroundBrush}" 
                BorderBrush="{StaticResource BorderBrush}" 
                BorderThickness="0,1,0,0" 
                Padding="10,5">
            <TextBlock Text="{Binding StatusMessage}" 
                      FontSize="12" 
                      Foreground="{StaticResource SecondaryTextBrush}"/>
        </Border>

        <!-- Loading Overlay -->
        <Border Grid.Row="0" Grid.RowSpan="5" 
               Background="#80000000" 
               Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="Loading..." 
                          FontSize="20" 
                          Foreground="White" 
                          HorizontalAlignment="Center" 
                          Margin="0,0,0,10"/>
                <ProgressBar IsIndeterminate="True" 
                            Width="200" 
                            Height="10"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
