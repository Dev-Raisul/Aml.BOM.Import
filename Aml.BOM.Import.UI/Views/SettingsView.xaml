<UserControl x:Class="Aml.BOM.Import.UI.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            
            <!-- Database Settings -->
            <GroupBox Header="Database Settings" Margin="0,0,0,20">
                <StackPanel Margin="10">
                    <TextBlock Text="Connection String:" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding DatabaseConnectionString, UpdateSourceTrigger=PropertyChanged}" 
                            Margin="0,0,0,10"/>
                    <Button Content="Test Connection" 
                           Command="{Binding TestConnectionCommand}"
                           Style="{StaticResource SecondaryButtonStyle}"
                           HorizontalAlignment="Left"/>
                </StackPanel>
            </GroupBox>

            <!-- Sage Settings -->
            <GroupBox Header="Sage Integration Settings" Margin="0,0,0,20">
                <StackPanel Margin="10">
                    <TextBlock Text="Server URL:" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding SageServerUrl, UpdateSourceTrigger=PropertyChanged}" 
                            Margin="0,0,0,10"/>
                    
                    <TextBlock Text="Username:" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding SageUsername, UpdateSourceTrigger=PropertyChanged}" 
                            Margin="0,0,0,10"/>
                    
                    <TextBlock Text="Company Code:" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding SageCompanyCode, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>
            </GroupBox>

            <!-- Report Settings -->
            <GroupBox Header="Report Settings" Margin="0,0,0,20">
                <StackPanel Margin="10">
                    <TextBlock Text="Output Directory:" Margin="0,0,0,5"/>
                    <DockPanel Margin="0,0,0,10">
                        <Button Content="Browse..." 
                               DockPanel.Dock="Right" 
                               Margin="5,0,0,0"
                               Style="{StaticResource SecondaryButtonStyle}"/>
                        <TextBox Text="{Binding ReportOutputDirectory, UpdateSourceTrigger=PropertyChanged}"/>
                    </DockPanel>
                    
                    <CheckBox Content="Auto-generate reports after integration" 
                             IsChecked="{Binding AutoGenerateReports}"/>
                </StackPanel>
            </GroupBox>

            <!-- Save Button -->
            <Button Content="Save Settings" 
                   Command="{Binding SaveSettingsCommand}"
                   Style="{StaticResource PrimaryButtonStyle}"
                   HorizontalAlignment="Left"
                   Margin="0,10,0,10"/>

            <!-- Status Message -->
            <TextBlock Text="{Binding StatusMessage}" 
                      Foreground="#27AE60"
                      FontWeight="Bold"
                      Visibility="{Binding StatusMessage, Converter={StaticResource StringToVisibilityConverter}}"/>

        </StackPanel>
    </ScrollViewer>
</UserControl>
